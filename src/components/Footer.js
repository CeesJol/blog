import React from 'react';

const Footer = () => (
	<footer className="footer">
		<div className="content-container">
		&copy; {new Date().getFullYear()} Cees Jol
		</div>
	</footer>
);

export default Footer;